"""Phase F gap tools — new tools for segment-specific analysis.

Each module in this package is a pure computation function with no DB or I/O
dependencies. The api.py executor resolves inputs (mortgage rates, etc.)
before calling the computation.
"""
